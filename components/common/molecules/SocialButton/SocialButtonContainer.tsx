import React from 'react';

import { SocialButton, SocialButtonProps } from './SocialButton';

export const SocialButtonContainer = ({ data }: SocialButtonProps) => {
  return <SocialButton data={data} />;
};
